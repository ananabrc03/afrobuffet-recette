---
interface Props {
  name: string;
  class?: string;
}

const { name, class: className = '' } = Astro.props;
---

<svg class:list={['icon', className]} aria-hidden="true" focusable="false">
  <use href={`/assets/icons/recipe-icons.svg#icon-${name}`}></use>
</svg>
